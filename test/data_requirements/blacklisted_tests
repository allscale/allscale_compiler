# Do not remove this file even if it does not blacklist any test.
# It is needed for the integration test driver to recognize this folder as the parent for multiple other tests.


### Not working until we fix stuff in the backend and/or can use the shared memory data item manager in the runtime
grid_stencil
grid_stencil_2d

####################################################################################
# The following tests are not really blacklisted but take rather long to execute.  #
# All test names preceded with a '+' will not be executed by default but only when #
# the integration test driver is run with the option to run these long tests also. #
####################################################################################
